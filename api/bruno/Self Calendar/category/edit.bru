meta {
  name: edit
  type: http
  seq: 4
}

patch {
  url: http://{{server_ip}}:{{server_port}}/category/{{category_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Updated category",
    "color": "#00B4D8"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
