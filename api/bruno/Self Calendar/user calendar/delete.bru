meta {
  name: delete
  type: http
  seq: 5
}

delete {
  url: http://{{server_ip}}:{{server_port}}/user_calendar/{{membership_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
