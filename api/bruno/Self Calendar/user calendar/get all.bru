meta {
  name: get all
  type: http
  seq: 1
}

get {
  url: http://{{server_ip}}:{{server_port}}/user_calendar/all/{{calendar_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
