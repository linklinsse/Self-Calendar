meta {
  name: change password
  type: http
  seq: 2
}

patch {
  url: http://{{server_ip}}:{{server_port}}/user/password
  body: json
  auth: inherit
}

body:json {
  {
    "old_password": "test",
    "new_password": "newpassword"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
