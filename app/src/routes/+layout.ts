// Required by @sveltejs/adapter-static:
// every page must be pre-renderable (or served from the SPA fallback).
export const prerender = true;
export const ssr = false; // Capacitor / SPA — no server-side rendering
